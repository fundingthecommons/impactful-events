# Test environment configuration
# Copy to .env.test and update values as needed.
#
# For integration tests, DATABASE_URL is set automatically by Testcontainers.
# You do NOT need to set it manually unless running tests without Docker.

SKIP_ENV_VALIDATION=1
NODE_ENV=test

# Only needed if running integration tests without Testcontainers:
# DATABASE_URL=postgresql://test:test@localhost:5432/test_db

# E2E base URL (for Playwright)
E2E_BASE_URL=http://localhost:3000
